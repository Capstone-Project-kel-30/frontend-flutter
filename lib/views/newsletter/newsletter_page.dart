import 'package:flutter/material.dart';

class NewsletterPage extends StatelessWidget {
  const NewsletterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: const [
            Text('Newsletter Under Development'),
          ],
        ),
      ),
    );
  }
}
