import 'package:flutter/material.dart';

class VideoContentPage extends StatelessWidget {
  const VideoContentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: const [
            Text('Video Content Under Development'),
          ],
        ),
      ),
    );
  }
}
